{
  "name": "sentry",
  "description": "",
  "scripts": {
    "postdeploy": "npm run seed-db"
  },
  "env": {
    "COOKIE_SECRET": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    },
    "REQUIRE_SSL": "true",
    "DOMAIN": "sentry.herokuapp.com"
  },
  "addons":[
    "mongolab"
  ]
}
